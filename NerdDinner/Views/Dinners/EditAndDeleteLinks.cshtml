@model NerdDinner.Models.Dinner

@{
    if (Model.IsHostedBy(Context.User.Identity.Name))
    {
        @Html.ActionLink("Edit", "Edit", new { id = Model.DinnerId }); <span> | </span>
        @Html.ActionLink("Delete Dinner", "Delete", new { id = Model.DinnerId });
    }
}